[build]
builder = "nixpacks"
buildCommand = "pnpm install --frozen-lockfile && pnpm --filter @telecomm/web build"

[deploy]
startCommand = "pnpm --filter @telecomm/web start"
healthcheckPath = "/"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
